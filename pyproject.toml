[build-system]
requires = ["setuptools",
    "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["version", "dependencies", "optional-dependencies"]
name = "lung_modelling"
[tool.setuptools_scm]